# MITRA API — runtime dependencies
#
# Deliberately minimal. No AI vendor SDK, no HTTP client to a model provider:
# all reasoning is local, so the service runs with no keys and no egress.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
PyJWT==2.10.1
python-multipart==0.0.20

# Development / test
pytest==8.3.4
httpx==0.28.1
